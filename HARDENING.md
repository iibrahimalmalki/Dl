# تقرير التحصين الأمني — منصّة دلو ورغوة
**التاريخ:** 1 أغسطس 2026 · مشروع Supabase: cnmggdrlkgsyrjxmvydv

---

## ✅ ما تم إنجازه (قاعدة البيانات — نُفِّذ فعلياً)

أُغلق وصول الزوّار (anon) على كل جداول المنصّة الحسّاسة، وأُبقي فقط ما تحتاجه الصفحات العامة.

| الجدول | وصول الزائر قبل | وصول الزائر بعد | السبب |
|--------|----------------|-----------------|-------|
| **الموظفون** employees | كل الصلاحيات (ALL) | إدراج/قراءة/تحديث فقط | نموذج الموظف العام يحتاجها — **مُنع الحذف** |
| **المتقدّمون** applicants | كل الصلاحيات + حذف + TRUNCATE | إدراج/قراءة/تحديث فقط | التقديم وإجابة المقابلة — **مُنع الحذف والمسح** |
| **جلسات المقابلة** interview_sessions | كل الصلاحيات (ALL) | قراءة/تحديث فقط | إجابة المرشّح — مُنع الإنشاء/الحذف |
| **استبيان المواهب** tma_invites | كل الصلاحيات | قراءة/تحديث فقط | تعبئة الاستبيان |
| الرواتب / العمليات / المخالفات / الجولات / التعاقد | — | **مُغلق تماماً** | لا يحتاجها الزائر |
| المستخدمون / الصلاحيات / المشغّلون / الفرق / المواهب TMA | متعرّض في الـschema | **مُغلق تماماً** | إدارية بحتة |

وصول الموظفين المصادَق عليهم لم يتغيّر — يبقى محكوماً بنظام الصلاحيات `has_perm` وفق الهيكل.

**تحقّق:** بيانات الرواتب والعمليات والموظفين المالية لم تعد ظاهرة لأي زائر مجهول. دوال الأمان (`is_owner`, `has_perm`, `my_biker_id`) تثبّت `search_path` (محصّنة ضد اختطاف المسار).

---

## ⚠️ إجراءات مطلوبة منك (لا أستطيع تنفيذها نيابةً عنك)

### 1) تدوير مفتاح GitHub — **عاجل**
مفتاح الوصول (ghp_…) ظهر عدة مرات في نص المحادثة، ويجب اعتباره مكشوفاً.
- ادخل GitHub ← Settings ← Developer settings ← Personal access tokens
- **ألغِ** المفتاح الحالي فوراً، وأنشئ مفتاحاً جديداً بصلاحية `repo` فقط
- زوّدني به عند الحاجة للنشر القادم (لن أطبعه في المحادثة بعد الآن)

### 2) كلمة مرور المالك
تأكّد أن حساب المالك في Supabase Auth يستخدم كلمة مرور قوية وفريدة (غير مشتركة مع أي خدمة).

### 3) تفعيل حماية كلمات المرور المسرّبة
Supabase Dashboard ← Authentication ← Policies ← فعّل **Leaked password protection**
(يمنع استخدام كلمات مرور ظهرت في تسريبات معروفة.)

---

## 🔶 توصيات لاحقة (اختيارية — لم تُنفَّذ لتجنّب كسر الوظائف)

- **حاوية الأدلة الميدانية** `field-evidence` عامّة وتسمح بالتصفح — يُفضّل تحويلها إلى خاصة مع روابط موقّعة (signed URLs) لاحقاً؛ يتطلب تعديل عرض الصور في الجولات وبوابة البايكر.
- **روابط عامة برمز مكشوف:** روابط المقابلة/الاستبيان/الموظف تعتمد على سرّية المعرّف (uuid). لتشديدها مستقبلاً يمكن إضافة رمز تحقّق منفصل مع صلاحية انتهاء.
- **مشروع مشترك:** قاعدة البيانات نفسها تضم جداول لتطبيقات أخرى (استشارات، تشخيص…) مفتوحة للزوّار — ليست تابعة لدلو ورغوة ولم ألمسها. يُستحسن فصل مشروع دلو ورغوة في قاعدة مستقلة عند التوسّع.

---

## 🔒 نموذج الأمان القائم (للمرجع)
- **المالك** (`is_owner`): وصول كامل لكل شيء.
- **الموظف المُعيَّن**: صلاحيات وحداته فقط عبر `user_permissions` المولّدة من منصبه في الهيكل.
- **البايكر**: صفوفه الخاصة فقط (`my_biker_id`).
- **المواهب TMA والمستخدمون**: مقصورتان على المالك، لا تُمنحان لأي منصب إطلاقاً.
- **الزائر**: التقديم وإجابة المقابلة/الاستبيان ونموذج الموظف — لا شيء غير ذلك.
